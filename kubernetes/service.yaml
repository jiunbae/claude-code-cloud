apiVersion: v1
kind: Service
metadata:
  name: claude-code-cloud
  labels:
    app: claude-code-cloud
spec:
  type: ClusterIP
  selector:
    app: claude-code-cloud
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
    - name: websocket
      port: 3001
      targetPort: 3001
      protocol: TCP
    - name: pty-api
      port: 3003
      targetPort: 3003
      protocol: TCP
