import LoginForm from '@/components/Auth/LoginForm';

export const metadata = {
  title: 'Sign In - Claude Code Cloud',
  description: 'Sign in to your Claude Code Cloud account',
};

export default function LoginPage() {
  return <LoginForm />;
}
