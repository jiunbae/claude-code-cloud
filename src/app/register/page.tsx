import RegisterForm from '@/components/Auth/RegisterForm';

export const metadata = {
  title: 'Create Account - Claude Code Cloud',
  description: 'Create a new Claude Code Cloud account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
